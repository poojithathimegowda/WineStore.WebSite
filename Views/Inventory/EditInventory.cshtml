@model WineStore.WebSite.Models.PurchaseManager.InventoryViewModel

@{
    ViewData["Title"] = "EditInventory";
}

<h1>EditInventory</h1>

<h4>InventoryViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditInventory">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Inventory_ID" class="control-label"></label>
                <input asp-for="Inventory_ID" class="form-control" />
                <span asp-validation-for="Inventory_ID" class="text-danger"></span>
            </div>
            @* <div class="form-group">
            <label asp-for="Product_ID" class="control-label"></label>
            <input asp-for="Product_ID" class="form-control" />
            <span asp-validation-for="Product_ID" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <select asp-for="SelectedProducts" class="form-control" asp-items="@(new SelectList(Model.ExistingProducts, "Value", "Text"))">
                    <option value="">Selected Product</option>
                </select>
            </div>
            @* <div class="form-group">
            @*<div class="form-group">
            <label asp-for="Shop_ID" class="control-label"></label>
            <input asp-for="Shop_ID" class="form-control" />
            <span asp-validation-for="Shop_ID" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <select asp-for="SelectedShop" class="form-control" asp-items="@(new SelectList(Model.ExistingShop, "Value", "Text"))">
                    <option value="">Selected Shop</option>
                </select>
            </div>
            <div class="form-group">
                <div class="form-group">
                    <label asp-for="Quantity" class="control-label"></label>
                    <input asp-for="Quantity" class="form-control" />
                    <span asp-validation-for="Quantity" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
